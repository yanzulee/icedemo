import React, { Component, Suspense } from 'react';

export default class ScreenLayout extends Component {
  static displayName = 'ScreenLayout';

  static propTypes = {};

  static defaultProps = {};

  render() {
    const { routerData } = this.props;

    return (
      <div>
        4321
      </div>
    );
  }
}
